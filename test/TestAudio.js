/*
var sinon = require('sinon');
var chai = require('chai');

chai.should();

describe('Test a Factories [Angular]', function(){
    
    beforeEach(module('Entidades'));
    
    var audio = undefined;
    var document = undefined;
    
    beforeEach(inject(function($audio, $document) {
        audio = $audio;
        document = $document;
        
        //sinon.stub(audio, 'warn', function() {});
    }));
    
    describe('Cuando es invocado', function(){
    
        it('deberia ser distinto de null y undefined', function(){
            console.log(audio);
        });
    });
});
*/